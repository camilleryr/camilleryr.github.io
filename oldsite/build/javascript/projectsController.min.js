/*! camilleryr.github.io 2017-11-02 */

let writeElement=document.getElementById("projectContent");const writeContent=e=>{let t="";e.forEach(e=>{let a="";e.techUsed.forEach(e=>{a+=`\n            <span class="tech">${e}</span>\n            `});let n="";e.teammates.forEach(e=>{n+=`\n            <span class="teammates">${e}</span>\n            `}),t+=`\n        <article class="projectItem">\n            <h2>Project Name : ${e.title}</h2>\n            <div class="techTags">${a}</div>\n            <div class="teammatesTags">${n}</div>\n            <h4>Completed on: ${e.dateCompleted}</h4>\n            <a href="${e.link}" target="_blank">Check it out</a>\n        </article>\n        `}),writeElement.innerHTML=t};window.onload=onPageLoad(projectDatabase,2);